# Your CodeGraphy theme

Replace this text with a clear description and screenshots of your theme. Keep `manifest.json`, `theme.css`, each mode preview, `LICENSE`, and `codegraphy-release.json` at stable public paths.

Use the [CodeGraphy registry author guide](https://github.com/joesobo/codegraphy-registry#readme) to build a package and submit the initial directory listing.
